## Setup (whole team, before splitting)

- [ ] Fork the repo, create branch under team name
- [ ] Scaffold Vite React + TypeScript app named `react-homework` (see `homework/react/README.md`)
- [ ] Push empty skeleton: `<Routes>` / `<Route>` wired for Home/About/Contact/Article with placeholder pages, plus empty `Navbar` / `Footer` — so everyone can branch off it
